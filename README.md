# Antivirus
Simple antivirus for windows

This AV will check signed and unsigned file against virustotal if they are trusted or not. If not, it will simply remove all permissions from that file.
It will also check for keyloggers and overlays and terminate those processes if found.
